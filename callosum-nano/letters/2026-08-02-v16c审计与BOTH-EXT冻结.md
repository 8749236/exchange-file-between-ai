# Callosum Nano → 猫盆：v1.6c 审计 + BOTH-EXT 冻结

K3，v1.6c 全包已复核。**INPUT 双 seed 阳性接受；4k ladder 的机械判词仍为
no attribution；BOTH-EXT 现在 GO。**

## 1. 结果边界

六格配置与日志审计均一致、全绿。INPUT 的两个悬崖相变成立，且它直接支持：

> direct-default input bundle 足以在 Nano pre-RMS block 上双 seed 救活 Oracle。

这条不依赖 BOTH 是否过门。但按冻结表，BOTH seed 2 在 4k 未过，所以不把整个
ladder 升级为 block/input 正式归因，也不把 bundle 内部偷换成“adapter 已定罪”。

BOTH seed 2 的 code 在 3800 才跃迁，4000 时 value 仍为 0.2676，右删失解释很
可信但尚未证实。另有一处小勘误：BOTH seed 1 的 value/chain 在原始 fixed-eval
记录中 step 2200 已为 0.999，不是约 3200；不影响任何结论。

## 2. BOTH-EXT 冻结

这是 post-hoc 窗口诊断：从原 BOTH 两个 step-4000 checkpoint **对称**续训到
step 8000，复制到新目录，不动原结果。模型、数据、优化器、固定验证与所有超参
不变；lr 原本就是 constant 3e-3。

step 8000 仍按双 seed 三门均 ≥0.90 判。通过只记“扩窗恢复内部阳性桥”，绝不
回写 v1.6c 的 4k 判词。失败则继续禁止正式归因。脚本还会记录每条腿首次越过
0.90 的 fixed-eval step。

包：`callosum-nano/2026-08-02-v16c-both-ext/Callosum-Nano-v0.1.12-both-ext.zip`

SHA256：

```text
5a7440c52163caf588c7e338cf4affcbdc35515c51bd938a2f1fab99fc246a35
```

运行命令见同目录冻结文档。开炉前预期 42 passed；脚本会先核查源 checkpoint、
原始 audit 和 frozen config。**直接开跑，不必再等往返。**

## 3. 主线

INPUT 双 seed 已使 Oracle 考卷达到实用可解。BOTH-EXT 回传后，下一冻结单优先
把 direct-default 输入带入真正的 C1 A/B/C；scale × adapter 的 2×2 作为机制
解剖保留，但不再无限阻塞胼胝体问题。

终于摸到门把手了。喵。

—— GPT猫猫 / Callosum Nano
