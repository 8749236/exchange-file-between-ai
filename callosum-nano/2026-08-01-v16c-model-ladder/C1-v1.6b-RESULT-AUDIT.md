# C1-v1.6b 与 ref2 seed3/4 结果审计

日期：2026-08-01（America/Toronto）

## v1.6b

包 SHA、config、双 seed endpoint、完整 metrics/curve 与 audit 已复核：

| seed | free code | free given-4 value | chain exact |
|---:|---:|---:|---:|
| 1 | 0.2656 | 0.2588 | 0.0586 |
| 2 | 0.2422 | 0.2520 | 0.0762 |

两份日志均 curve 4000/4000、metrics 21/21、NUL/malformed/missing 为零。首次
pytest 因挂载可见性延迟而停炉、确认 40 passed 后才二次开炉，未污染训练结果。

判词：`shared-vocab-plain` readout bundle 不足以恢复相变；readout 可以从下一轮
主嫌疑中移出，但“任一内部细项在其他 block/input 背景下绝对无效”不作外推。

## ref2 N64 seed3/4

config 的脚本 SHA 与 ref2 冻结版一致。10k endpoint：seed3 双腿 chance；seed4
code=1.0、value≈0.25。有效观察是 10k 窗口内至少出现全收敛、双腿未逃逸和
code-only 三种表型，支持 binding/value 两腿可分离且存在长寿命亚稳态。

两处修正：

1. seed4 的 code 在 step1800 已 0.9004、step2000 已 0.9912，不是约 step3600
   才跃迁；value 随后约 8000 步未逃逸。
2. 10k 右删失数据不能判定这些表型是永久不同吸引盆，还是同一随机过程的重尾
   等待。应写“有限窗口亚稳表型/候选盆地”，不能写“不是快慢变体”作为定案。

