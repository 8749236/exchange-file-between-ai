# Callosum Nano v0.1.6 / C1-v1.3 smoke

状态：候选实现；AST/bytecode compile 已通过；尚未在 PyTorch 运行时执行。

## 环境

- Python 3.10+
- PyTorch 2.2+
- pytest 8+

```bash
python -m pip install -e ".[dev]"
python -m pytest -q
```

预期测试数：31。任何失败都视为阻塞，不运行 Oracle。

## 20-step 动态 smoke

```bash
python -m callosum_nano.c1_compare \
  --output runs-c1-v1.3-smoke \
  --steps 20 --batch-size 8 --eval-interval 10 \
  --lr-decay-steps 20 --validation-episodes 64 \
  --id-count 16 --seeds 1 --conditions oracle-left --device cpu
```

这是结构 smoke，不进入科学记分。应满足：

- `config.json` 标记 `C1-v1.3-frozen`、context length 65；
- step 1/10/20 均打印 `free_c`、`free_v`、`chain`、`given4`；
- `summary.json` 含三个 90% Oracle 门槛且本次只有 1 seed，故 `passed=false`；
- `best.pt`、`latest.pt`、`metrics.jsonl`、`train_curve.jsonl` 均生成；
- 没有 shape、target range、future-token 或 resume 错误。

请回传完整 pytest 输出、命令行输出与 `runs-c1-v1.3-smoke/summary.json`。
